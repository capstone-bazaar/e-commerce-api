extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Query {
  me: User
  findAllUsers: [User]
}

type Mutation {
  addUser(fields: UserInput): User
  updateUserById(fields: UserInput): User
  deleteUserById(id: String): User
  login(email: String, password: String): String
  register(
    fullName: String
    phone: String
    avatarURL: String
    password: String
    email: String
  ): User
}

type User @key(fields: "id") {
  id: ID
  fullName: String
  phone: String
  avatarURL: String
  password: String
  email: String
  address: String
  createdAt: String
  updatedAt: String
}

input UserInput {
  id: ID
  fullName: String
  phone: String
  avatarURL: String
  password: String
  email: String
  address: String
}
